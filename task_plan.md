# 任务计划：10 个问题现状复核

## 目标
检查用户列出的 10 个问题在当前项目中是否仍然存在，并给出代码 / 配置 / 数据库层面的证据。此次任务以核查为主，不主动修改业务代码。

## 当前阶段
阶段 4

## 各阶段
### 阶段 1：恢复上下文与建立核查范围
- [x] 读取 `planning-with-files-zh` 技能说明
- [x] 读取现有 `task_plan.md`、`findings.md`、`progress.md`
- [x] 修正规划文件目标，切换到“10 个问题现状复核”
- **状态：** complete

### 阶段 2：逐项定位代码与配置
- [x] 定位 Bug 1 对应的 Standardizer 并发实现
- [x] 定位 Bug 2 对应的 LLM 默认模型解析逻辑
- [x] 定位 Bug 3 对应的 Supervisor 去重逻辑
- [x] 定位 Bug 4 对应的 `None` guard
- [x] 定位 Bug 5 / 7 / 8 对应的持久化、事务与 CVSS 归一化逻辑
- [x] 定位 Bug 6 对应的 dedup 索引增量写入逻辑
- [x] 定位 Bug 9 对应的 schema / migration 现状
- [x] 定位 Bug 10 对应的 Windows 编码防护
- **状态：** complete

### 阶段 3：必要的只读验证
- [x] 检查测试、脚本或 SQL 文件是否支持上述修复结论
- [x] 如有必要，做只读数据库 / 配置核对
- **状态：** complete

### 阶段 4：汇总结论
- [x] 为 10 个问题分别给出“已修复 / 仍存在 / 部分存在”的判断
- [x] 记录剩余风险和证据位置
- [ ] 输出简明结论
- **状态：** in_progress

## 关键问题
1. 当前仓库中的代码是否已经落实了这 10 个问题对应的修复，而不是只在某次运行中被临时绕过？
2. 是否存在“主路径看起来正常，但旁路、默认值、脚本或迁移仍留有旧问题”的情况？
3. 哪些问题已经彻底修复，哪些只是部分缓解或依赖外部环境？

## 已做决策
| 决策 | 理由 |
|------|------|
| 本轮以只读核查为主 | 用户要求是“检查有没有存在着 10 个问题” |
| 优先查代码，其次查测试和配置 | 代码是最直接的当前状态证据 |
| 如发现结论依赖数据库现状，再做只读验证 | 避免无必要的外部依赖操作 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 旧 planning 文件仍指向上一轮修复任务 | 1 | 已重写为当前“10 个问题现状复核”任务 |
| 技能文档给出的 `session-catchup.py` 默认路径失效 | 1 | 改用实际存在的 `C:\\Users\\sven3\\.agents\\skills\\planning-with-files-zh\\scripts\\session-catchup.py` |

## 备注
- 当前工作目录：`e:\@4C-2026\SUFE-SAADS-Qwen`
- 本轮不主动修改业务代码，除非核查过程遇到必须修复才能继续的阻塞
- 结论基于当前工作区（working tree）而非某个已提交 tag；相关后端文件当前存在未提交改动
