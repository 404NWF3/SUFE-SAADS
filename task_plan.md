# 任务计划：saads_wp12 接入 dashboard 渗透测试智能体

## 目标
设计一套完整、可落地的方案，将 `backend/agents/saads_wp12` 的能力接入 dashboard 中的“渗透测试智能体（WP1-2）”模块。

## 当前阶段
阶段 1

## 各阶段

### 阶段 1：现状梳理与能力盘点
- [x] 理解用户目标
- [x] 梳理 `saads_wp12` 的输入、执行链路、输出产物
- [x] 梳理 dashboard 中 `wp12` 页面的现有结构与能力缺口
- [x] 将关键发现记录到 `findings.md`
- **状态：** complete

### 阶段 2：接入方案设计
- [ ] 设计前端交互与页面结构
- [ ] 设计后端 API、运行编排与状态流
- [ ] 设计数据模型、产物展示与错误处理
- [ ] 记录核心技术决策
- **状态：** in_progress

### 阶段 3：实施路径与验收
- [ ] 输出分阶段实施步骤
- [ ] 输出联调与测试建议
- [ ] 输出风险、依赖与上线建议
- **状态：** pending

## 关键问题
1. `saads_wp12` 当前是否已有可复用的 API / 持久化能力，还是仅有内部 graph 运行链路。
2. dashboard 里的 `wp12` 现在是否只是占位页，还是已经接入了通用运行面板。
3. “接入 dashboard 的渗透测试智能体部分”应优先做方案生成体验，还是同时暴露作业历史、测试包详情和流式日志。

## 已做决策
| 决策 | 理由 |
|------|------|
| 使用 `planning-with-files-zh` 方式记录分析过程 | 这是一个多阶段方案设计任务，需要持续沉淀上下文 |
| 先基于现有代码反推接入方案，再给实施清单 | 这样能保证方案贴合当前项目结构 |
| `wp12` 接入优先参考 `sentinel` 的“定制 dashboard 内容页”模式，而不是继续沿用通用占位页 | `wp12` 需要表单、结果渲染、产物查看和运行历史，通用页承载不了 |
| `saads_wp12` 的前端结果契约优先使用 `presentation_state` + `plan markdown` | 后端已经具备这两类产物，复用成本最低 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `Get-Content frontend/src/app/dashboard/[wp]/page.tsx` 因 `[]` 被当作通配符而失败 | 1 | 后续对该路径使用 `-LiteralPath` |

## 备注
- 优先输出贴合现有仓库结构的接入设计，而不是抽象架构。
- 如果发现现有后端路由或前端 hook 已经部分覆盖 `wp12`，优先复用并标注改造点。
- 当前更像“`wp12` 前端入口已注册，但后端专用 API 尚未落地”的中间态。
