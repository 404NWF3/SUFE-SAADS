# 任务计划：WP1-1 阿里模型适配故障审查

## 目标
全面审查项目中与 WP1-1 相关的代码与运行痕迹，定位阿里大模型运行时降级为 `"rules_only_degraded"` 的根因，并给出可执行修复方案。

## 当前阶段
阶段 5

## 各阶段

### 阶段 1：范围确认与证据收集
- [x] 理解用户问题与已知现象
- [x] 确认 WP1-1 相关代码范围
- [x] 阅读 `run_main.txt` 与 `.runtime` 关键信息
- [x] 将初步发现记录到 `findings.md`
- **状态：** complete

### 阶段 2：执行链路审查
- [x] 梳理 `main.py` 到 WP1-1 策略执行链路
- [x] 审查模型适配、提供商路由、降级逻辑
- [x] 对比 OpenAI 正常路径与阿里失败路径
- **状态：** complete

### 阶段 3：原因确认与验证
- [x] 确认导致阿里模型不可用的直接触发点
- [x] 确认是否存在配置、协议、解析或超时等次级问题
- [x] 运行最小化调试验证
- **状态：** complete

### 阶段 4：修复方案设计与落地
- [x] 给出最小修复方案
- [x] 实现 provider 兼容修复
- [x] 补充回归测试
- **状态：** complete

### 阶段 5：结论输出
- [x] 形成按严重度排序的 review findings
- [x] 给出文件定位、原因解释、解决方案
- [x] 说明未验证项与残余风险
- **状态：** in_progress

## 关键问题
1. 阿里模型请求是在“发不出去”、“发出后报错”还是“返回结果后解析失败”？
2. `"rules_only_degraded"` 是由 provider 适配失败触发，还是由上层策略执行器误判触发？
3. OpenAI 正常、阿里异常是否说明代码默认假设了 OpenAI 风格的结构化输出能力？

## 已做决策
| 决策 | 理由 |
|------|------|
| 先审日志与运行痕迹，再回溯代码 | 先确定真实失败路径，避免在全项目中盲搜 |
| 审查范围聚焦到 WP1-1 及其模型调用链 | 用户的问题是特定工作包与提供商适配，不是全仓库泛查 |
| 为 DashScope/Qwen 单独加结构化输出适配层 | 同一兼容问题分散在 7 个工具文件中，集中修复更稳妥 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `python main.py -live` 参数无效 | 1 | 已验证 CLI 仅支持 `--live` |
| DashScope Qwen 在 thinking mode 下拒绝 LangChain 强制 `tool_choice` | 1 | 已新增 provider 兼容层，默认对 DashScope Qwen3/Qwen3.5 关闭 thinking |

## 备注
- 最终输出以 code review 形式呈现，优先给出 findings、根因与修复建议。
- 如果发现用户本地配置也参与触发，会明确区分“代码缺陷”与“环境配置问题”。
