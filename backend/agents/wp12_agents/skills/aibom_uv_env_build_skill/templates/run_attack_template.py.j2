"""Default attack runner generated by aibom_uv_env_build_skill."""

from __future__ import annotations

import json


def main() -> None:
    payload = {
        "environment_id": "${environment_id}",
        "tenant_id": "${tenant_id}",
        "scenario_id": "${scenario_id}",
        "attack_id": "${attack_id}",
        "status": "runner_ready",
    }
    print(json.dumps(payload))


if __name__ == "__main__":
    main()
