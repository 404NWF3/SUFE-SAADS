[project]
name = "${project_name}"
version = "0.1.0"
description = "AIBOM uv environment workspace"
requires-python = ">=${python_version},<${python_version}.99"
dependencies = [
${dependency_block}
]

[tool.uv]
package = false
